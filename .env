# macOS external volume (e.g. /Volumes/) generates ._* resource fork files
# that BuildKit's sender cannot read xattr on. Disable BuildKit to work around it.
DOCKER_BUILDKIT=0
COMPOSE_DOCKER_CLI_BUILD=0
